# NEVER commit real private keys.
# Copy this file to `.env` and fill in values locally.

# RPC URLs
BASE_RPC_URL=
SEPOLIA_RPC_URL=

# Deployer account private key (0x-prefixed optional)
PRIVATE_KEY=

# Optional: if you already deployed EscrowVault, set it here to reuse it.
VAULT_ADDRESS=

# Optional deployment parameters
# If not set, the deployer will be used as the fee recipient.
FEE_RECIPIENT=

# Optional arbiter (can be left empty to disable arbiter actions)
ARBITER=

# Protocol fee in basis points (bps). Default in contract is 250 (=2.5%). Max allowed: 1000.
PROTOCOL_FEE_BPS=